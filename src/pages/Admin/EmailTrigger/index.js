import React from "react";
import Layout from "../../../layout/Layout";
import Options from "./components/Options/Options";
const index = () => {
  return <Layout component={<Options />} />;
};

export default index;
